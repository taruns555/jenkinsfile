pipeline {
      agent any
      stages {
         stage ('testing stage') {
             steps{
                  echo 'test1'
            sh 'mkdir from-jenkins'
            sh 'touch from-jenkins/test.txt'
                  }
           }
          
        }
    }
